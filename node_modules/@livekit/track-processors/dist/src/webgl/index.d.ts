import { MPMask } from '@mediapipe/tasks-vision';
export declare const setupWebGL: (canvas: OffscreenCanvas) => {
    render: (frame: VideoFrame, mask: MPMask) => void;
    setBackgroundImage: (image: ImageBitmap | null) => Promise<void>;
    setBlurRadius: (radius: number | null) => void;
    cleanup: () => void;
} | undefined;
